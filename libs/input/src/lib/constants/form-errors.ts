import { FormErrors } from '../interfaces/form-errors.interface';

export const GLOBAL_FORM_ERRORS: FormErrors = {
  addProjectForm: {
    name: {
      required: 'Project name is required',
    },
  },
};
